params.CONDA_ENVS_PATH = "../setup/conda_envs"
params.DATABASE_DIR="../setup/databases"


conda {
    cacheDir = "${params.CONDA_ENVS_PATH}"
    useMamba = true 
    enabled = true   // Ensure Conda is enabled for processes
}
summary {
    enabled = false
}